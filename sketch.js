// The Array of Point
let pointArray = [];

// The Array of Line
let lineArray = [];

// The Array of SemiCircle
let semiCircleArray = [];

function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
}
